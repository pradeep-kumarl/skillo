version: 1
applications:
  - appRoot: skillo-app
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 20
            - node -v
            - npm install
        build:
          commands:
            - npx expo export -p web
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
